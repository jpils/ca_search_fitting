[project]
name = "dft_energy_fitting"
version = "0.1.0"
description = "ca_search"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "scipy",
    "numpy",
]

[tool.uv]
managed = true
